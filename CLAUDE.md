# CLAUDE.md

**Read [`AGENTS.md`](AGENTS.md) before doing anything in this repository.** It
carries the rules that are never negotiable — run the tests after every change,
never fail silently, `apply` is destructive, and no customer data lands here.

Then read [`.agents/agents.md`](.agents/agents.md), the full engineering guide:
the hierarchy grammar, what "compliant" means for each resource type, the
iteration cadence rules, authentication, and the source layout.

`.agents/agents.md` is the single source of truth for guidance. Put new guidance
there, not in this file.
